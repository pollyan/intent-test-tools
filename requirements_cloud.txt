# 轻量级云端执行所需的依赖包 - 针对免费云服务器优化

# 异步HTTP客户端（轻量级）
aiohttp==3.9.1
aiofiles==23.2.0

# 基础HTTP请求
requests==2.31.0

# 异步支持
asyncio==3.4.3

# 日期时间处理
python-dateutil==2.8.2

# 环境变量管理
python-dotenv==1.0.0

# JSON处理增强
ujson==5.8.0

# 进程管理
psutil==5.9.0

# 图像处理（轻量级）
pillow==10.1.0

# 数据序列化
msgpack==1.0.7

# 注意：
# - 移除了playwright依赖，使用MidSceneJS的Node.js实现
# - 减少了大型AI包的依赖
# - 优化了包版本以减少冲突
# - 总包体积控制在50MB以内
